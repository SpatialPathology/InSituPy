---
description: Plan a change (read-only) and write a self-contained implementation report to .log/. Runs on whatever model this session is currently using - use /plan-opus or /plan-fable to pin a specific stronger model.
argument-hint: <goal or task description>
allowed-tools: Read, Glob, Grep, Write, WebSearch, WebFetch, PowerShell, mcp__insitupy__*
---

# /plan — design an implementation, write a durable report

@.claude/plan-protocol.md

## Task

$ARGUMENTS
