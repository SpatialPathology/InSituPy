# Data I/O — Saving and Loading

How to save and reload `InSituData` and `InSituExperiment` objects — full saves,
modality-specific partial saves, and reloading individual components.

```{eval-rst}
.. card:: Saving and Loading Data
    :link: InSituPy_save_load
    :link-type: doc

    Full reference for all save and load methods on ``InSituData`` and
    ``InSituExperiment``, including modality-specific partial saves
    (``save_geometries()``, ``save_cells()``, ``save_images()``) and
    practical guidance on when to use each.
```

```{toctree}
:hidden: true
:maxdepth: 1

InSituPy_save_load
```
