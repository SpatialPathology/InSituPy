# AI assistant integration

InSituPy ships a skill and an MCP server so AI assistants write correct InSituPy code instead of
guessing the API from memory.

The section below is included verbatim from the repository's `README.md`, so the two never drift
apart. Edit it there, between the `ai-integration-start` and `ai-integration-end` markers.

## Setup options

<!-- This heading exists so the included `###` headings nest consecutively under it. Removing it
     makes the document jump h1 -> h3, which MyST reports as a `myst.header` warning. -->

```{include} ../../README.md
:start-after: <!-- ai-integration-start -->
:end-before: <!-- ai-integration-end -->
```
